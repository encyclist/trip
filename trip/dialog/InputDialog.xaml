<Window x:Class="trip.dialog.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:trip.dialog"
        mc:Ignorable="d"
        Title="设置标题" Height="130" Width="300" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox Grid.Row="0" Grid.ColumnSpan="3" Padding="10,5,10,5" x:Name="textBox" MaxLines="1"/>
        <Button Grid.Row="1" Grid.Column="1" Padding="10,5,10,5" Margin="10,10,0,0" Content="确定" Click="OnClick_Done" />
        <Button Grid.Row="1" Grid.Column="2" Padding="10,5,10,5" Margin="0,10,0,0" Content="取消" Click="OnClick_Cancel" />
    </Grid>
</Window>
